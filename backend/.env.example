# Example env for backend (copy to backend/.env or set env vars in docker-compose)
DB_HOST=ibs-db
DB_NAME=ibs_core
DB_USER=ibs_user
DB_PASSWORD=ibs_pass
PORT=3000
BACKEND_JWT_SECRET=ChangeThisSecret

